# Schedule Page Images

Add a cover image for the schedule page:

- `schedule-cover.jpg` - Main cover image for `/schedule` page

Recommended size: 1200x630px
Optimize images to <300KB using the site's image optimizer:

```bash
python3 optimize_images.py
```
